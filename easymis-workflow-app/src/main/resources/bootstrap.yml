spring:
  application:
    name: easymis-workflow-app
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8888

  rabbitmq:
        addresses: amqp://localhost:5672
        username: alan
        password: alan

encrypt:
  failOnError: false

org:
  easymis:
    leads:
      name: test
      title: test